# WibeStore Frontend - Environment Variables
# Copy to .env and fill in values

# API (relative path for Vite proxy in dev; full URL for production)
VITE_API_BASE_URL=/api/v1

# WebSocket (for production use full URL; in dev Vite proxy handles /ws)
VITE_WS_BASE_URL=ws://localhost:8000

# Google OAuth
VITE_GOOGLE_CLIENT_ID=

# Sentry (optional)
VITE_SENTRY_DSN=

# Environment
# VITE_APP_ENV=development
