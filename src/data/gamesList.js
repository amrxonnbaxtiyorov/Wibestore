/**
 * WibeStore — o'yinlar faqat backend (API) dan keladi.
 * Lokal ro'yxat ishlatilmaydi.
 */

/** Bo'sh ro'yxat — barcha o'yinlar API dan olinadi */
export function getGamesList() {
  return [];
}

export default getGamesList;
